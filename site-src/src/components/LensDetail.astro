---
import { getEntry } from 'astro:content';
import Icon from './Icon.astro';
import { STATUS_LABEL } from '../lib/lenses';

interface Props {
  id: string;
}
const { id } = Astro.props;

const entry = await getEntry('lenses', id);
if (!entry) {
  throw new Error(`LensDetail: no lens entry found for id "${id}"`);
}
const { name, status, color, category, detail, version, releaseDate } = entry.data;
if (!detail) {
  throw new Error(`LensDetail: lens "${id}" has no detail copy`);
}

const base = import.meta.env.BASE_URL.replace(/\/+$/, '');
const statusLabel = STATUS_LABEL[status];
const isInstalled = status === 'installed';
const isComingSoon = status === 'coming-soon';

const heroLight = `${base}/lens-hero-${id}-light.png`;
const heroDark = `${base}/lens-hero-${id}-dark.png`;

const RAW = 'https://raw.githubusercontent.com/microsoft/CopilotScope/main/';
const encodePath = (p: string) => p.split('/').map(encodeURIComponent).join('/');
const tierAccents = ['ta', 'tb', 'tc'];
const tiers = detail.tiers.map((t, i) => ({ ...t, rawUrl: RAW + encodePath(t.pbit), accent: tierAccents[i % tierAccents.length] }));

const { stats, whatItShowsLead, features, valueFlow, valueFlowNote, whatsInsideLead, pageGroups, buildCards, dataSources, dataNote, getNote } = detail;

const navItems = [
  { href: '#ld-top', label: 'Intro' },
  { href: '#what-it-shows', label: 'What it shows' },
  { href: '#whats-inside', label: "What's inside" },
  { href: '#data-requirements', label: 'Data requirements' },
  ...(isComingSoon ? [] : [{ href: '#get-this-lens', label: 'Get this lens' }]),
];

const modalTitleId = `fb-title-${id}`;
const modalDescId = `fb-desc-${id}`;
---

<article class="lens-detail" data-lens={id} data-status={status}>
  {!isComingSoon && (
    <div class="ld-strip" data-ld-strip hidden={isInstalled}>
      <span class="ld-strip-id">
        <span class="tile-icon ld-strip-icon" style={`--tile-c: var(--${color}); background: var(--tile-c)`} aria-hidden="true"></span>
        <span class="ld-strip-name">{name}</span>
        <span class:list={['pill', `pill--${status}`]}>{statusLabel}</span>
      </span>
      <span class="ld-strip-right">
        <span class="star-row star-row--compact" data-lens={id}>
          <button class="star-btn" type="button" data-lens={id} data-name={name} aria-label={`Star ${name}`} aria-pressed="false"><span class="star-glyph" aria-hidden="true">&#9734;</span></button>
          <span class="star-count" data-lens={id} aria-live="polite">&#8212;</span>
        </span>
        {isInstalled && (
          <button class="ld-about-tab" type="button" data-ld-toggle aria-pressed="false">About this lens</button>
        )}
      </span>
    </div>
  )}

  {isInstalled && (
    <div class="ld-dashboard" data-ld-dashboard hidden>
      <div class="ld-empty">
        <span class="ld-empty-art" style={`--tile-c: var(--${color})`} aria-hidden="true">
          <Icon name="bar-chart" size={60} />
        </span>
        <h2 class="ld-empty-title">Your embedded dashboard will live here</h2>
        <p class="ld-empty-text">The live, in-page {name} report is on its way. When it ships, your interactive dashboard will appear right here in CopilotScope.</p>
        <a class="ld-empty-link" href="#ld-top" data-ld-about>
          <Icon name="help" size={16} /> Learn more about this lens
        </a>
      </div>
    </div>
  )}

  <div class="ld-marketing" data-ld-marketing>
    <div class="ld-layout">
      <div class="ld-main">
        <div class="ld-header-row">
          <div class="ld-hero" id="ld-top">
            <img class="ld-hero-img ld-hero-light" src={heroLight} alt="" />
            <img class="ld-hero-img ld-hero-dark" src={heroDark} alt="" />
          </div>
          <div class="ld-header-text">
            <header class="ld-title">
              <div class="ld-title-top">
                <h1>{name}</h1>
                <span class:list={['pill', `pill--${status}`]}>{statusLabel}</span>
              </div>
              <p class="ld-cat">{category}</p>
            </header>
          </div>
        </div>
        <p class="ld-intro">{detail.heroIntro}</p>

        {stats && (
          <div class="ld-stats">
            {stats.map((s) => (
              <div class="ld-stat">
                <span class="ld-stat-value">{s.value}</span>
                <span class="ld-stat-label">{s.label}</span>
              </div>
            ))}
          </div>
        )}

        <section class="ld-card" id="what-it-shows">
          <div class="ld-card-head">
            <span class="ld-card-icon ld-icon-shows" aria-hidden="true"><Icon name="eye" size={18} /></span>
            <h2>What it shows</h2>
          </div>
          {whatItShowsLead && <p class="ld-lead">{whatItShowsLead}</p>}
          {features ? (
            <ul class="ld-feature-grid">
              {features.map((f) => (
                <li class="ld-feature">
                  <span class="ld-feature-icon" aria-hidden="true"><Icon name={f.icon} size={18} /></span>
                  <div class="ld-feature-body">
                    <p class="ld-feature-title">{f.title}</p>
                    <p class="ld-feature-desc">{f.desc}</p>
                  </div>
                </li>
              ))}
            </ul>
          ) : (
            <p class="ld-prose">{detail.whatItShows}</p>
          )}
          {valueFlow && (
            <div class="ld-flow-wrap">
              <p class="ld-flow-title">How the value model works</p>
              <ol class="ld-flow">
                {valueFlow.map((step, i) => (
                  <li class="ld-flow-step">
                    <span class="ld-flow-node">{step}</span>
                    {i < valueFlow.length - 1 && <span class="ld-flow-arrow" aria-hidden="true"><Icon name="chevron" size={14} /></span>}
                  </li>
                ))}
              </ol>
              {valueFlowNote && <p class="ld-note"><Icon name="info" size={15} /> {valueFlowNote}</p>}
            </div>
          )}
        </section>

        <section class="ld-card" id="whats-inside">
          <div class="ld-card-head">
            <span class="ld-card-icon ld-icon-inside" aria-hidden="true"><Icon name="grid" size={18} /></span>
            <h2>What&#39;s inside</h2>
          </div>
          {whatsInsideLead && <p class="ld-lead">{whatsInsideLead}</p>}
          {pageGroups ? (
            <div class="ld-pages">
              {pageGroups.map((g) => (
                <div class="ld-page-group">
                  {g.label && <p class="ld-group-label">{g.label}</p>}
                  <ul class="ld-page-grid">
                    {g.items.map((it) => (
                      <li class="ld-page-item">
                        <span class="ld-page-icon" aria-hidden="true"><Icon name={it.icon} size={16} /></span>
                        <span class="ld-page-name">{it.name}</span>
                      </li>
                    ))}
                  </ul>
                  {g.note && <p class="ld-note"><Icon name="info" size={15} /> {g.note}</p>}
                </div>
              ))}
            </div>
          ) : buildCards ? (
            <div class="ld-build-cards">
              {buildCards.map((b) => (
                <div class="ld-build-card">
                  <div class="ld-build-head">
                    {b.icon && <span class="ld-build-icon" aria-hidden="true"><Icon name={b.icon} size={18} /></span>}
                    <p class="ld-build-name">{b.name}</p>
                    {b.badge && <span class="ld-badge">{b.badge}</span>}
                  </div>
                  <ul class="ld-checks">
                    {b.checks.map((c) => (
                      <li class="ld-check"><span class="ld-check-icon" aria-hidden="true"><Icon name="check" size={15} /></span>{c}</li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>
          ) : (
            <p class="ld-prose">{detail.whatsInside}</p>
          )}
        </section>

        <section class="ld-card" id="data-requirements">
          <div class="ld-card-head">
            <span class="ld-card-icon ld-icon-data" aria-hidden="true"><Icon name="database" size={18} /></span>
            <h2>Data requirements</h2>
          </div>
          {dataSources ? (
            <div class="ld-table">
              <div class="ld-tr ld-tr-head">
                <span class="ld-th ld-th-source">Source</span>
                <span class="ld-th ld-th-desc">Description</span>
                <span class="ld-th ld-th-req">Required</span>
              </div>
              {dataSources.map((s) => (
                <div class="ld-tr">
                  <span class="ld-td ld-td-source">
                    <span class="ld-src-icon" aria-hidden="true"><Icon name={s.icon} size={16} /></span>
                    <span class="ld-src-name">{s.source}</span>
                  </span>
                  <span class="ld-td ld-td-desc">{s.description}</span>
                  <span class="ld-td ld-td-req"><span class:list={['ld-req-pill', s.requirement === 'Required' ? 'ld-req-required' : 'ld-req-optional']}>{s.requirement}</span></span>
                </div>
              ))}
              {dataNote && (
                <div class="ld-tr ld-tr-note">
                  <span class="ld-td-note"><Icon name="info" size={15} /> {dataNote}</span>
                </div>
              )}
            </div>
          ) : (
            <p class="ld-prose">{detail.dataRequirements}</p>
          )}
        </section>

        {!isComingSoon && (
          <section class="ld-card" id="get-this-lens">
            <div class="ld-card-head">
              <span class="ld-card-icon ld-icon-get" aria-hidden="true"><Icon name="download" size={18} /></span>
              <h2>Get this lens</h2>
            </div>
            <p class="ld-lead ld-get-intro">{detail.getIntro}</p>
            <ul class="ld-tiers">
              {tiers.map((tier) => (
                <li class:list={['ld-tier', `ld-tier--${tier.accent}`]}>
                  <div class="ld-tier-head">
                    {tier.icon && <span class="ld-tier-icon" aria-hidden="true"><Icon name={tier.icon} size={20} /></span>}
                    <p class="ld-tier-name">{tier.name}</p>
                    {tier.badge && <span class="ld-badge">{tier.badge}</span>}
                  </div>
                  {tier.description && <p class="ld-tier-desc">{tier.description}</p>}
                  {tier.checks && (
                    <ul class="ld-checks">
                      {tier.checks.map((c) => (
                        <li class="ld-check"><span class="ld-check-icon" aria-hidden="true"><Icon name="check" size={15} /></span>{c}</li>
                      ))}
                    </ul>
                  )}
                  <a class="ld-dl-btn" href={tier.rawUrl} download>
                    <Icon name="download" size={16} /> Download template
                  </a>
                </li>
              ))}
            </ul>
            {getNote && <p class="ld-note ld-get-note"><Icon name="info" size={15} /> {getNote}</p>}
          </section>
        )}

        <p class="ld-disclaimer">{detail.disclaimer}</p>
      </div>

      <aside class="ld-rail" aria-label="Page information">
        <nav class="ld-rail-card ld-rail-nav" data-ld-rail-nav aria-label="On this page">
          <p class="ld-rail-title">On this page</p>
          <ul>
            {navItems.map((n) => (
              <li><a href={n.href} data-spy>{n.label}</a></li>
            ))}
          </ul>
        </nav>

        <div class="ld-rail-card ld-status-card">
          <p class="ld-rail-title">Product status</p>
          <p class="ld-status-line"><span class:list={['pill', `pill--${status}`]}>{statusLabel}</span></p>
          <dl class="ld-meta">
            <div><dt>Version</dt><dd>{version}</dd></div>
            <div><dt>Released</dt><dd>{releaseDate}</dd></div>
          </dl>
        </div>

        <div class="ld-rail-card ld-fb-card">
          <p class="ld-rail-title">Star &amp; feedback</p>
          <span class="star-row" data-lens={id}>
            <button class="star-btn" type="button" data-lens={id} data-name={name} aria-label={`Star ${name}`} aria-pressed="false"><span class="star-glyph" aria-hidden="true">&#9734;</span></button>
            <span class="star-count" data-lens={id} aria-live="polite">&#8212;</span>
            <span class="ld-fb-star-label">stars</span>
          </span>
          <button class="ld-fb-btn" type="button" data-fb-open>Send feedback</button>
          <span class="ld-fb-status" data-fb-status hidden></span>
        </div>
      </aside>
    </div>
  </div>

  <div class="fb-modal" data-fb-modal hidden>
    <div class="fb-backdrop" data-fb-backdrop></div>
    <div class="fb-dialog" data-fb-dialog role="dialog" aria-modal="true" aria-labelledby={modalTitleId} aria-describedby={modalDescId}>
      <div class="fb-head">
        <h2 id={modalTitleId} class="fb-title">Send feedback</h2>
        <button class="fb-x" type="button" data-fb-cancel aria-label="Close feedback">&#10005;</button>
      </div>
      <label class="fb-label" for={`fb-cat-${id}`}>Type of feedback</label>
      <select class="fb-select" id={`fb-cat-${id}`} data-fb-category>
        <option value="broken">Something is broken</option>
        <option value="confusing">Confusing or unclear</option>
        <option value="missing-info">Missing information</option>
        <option value="idea">Idea or suggestion</option>
        <option value="other" selected>Other</option>
      </select>
      <label class="fb-label" for={`fb-msg-${id}`}>Your message</label>
      <textarea class="fb-textarea" id={`fb-msg-${id}`} data-fb-message rows="5" maxlength="4000" placeholder="What's on your mind?"></textarea>
      <div class="fb-meta">
        <span class="fb-counter" data-fb-counter>0 / 4000</span>
      </div>
      <p class="fb-safety">Please don&#39;t include secrets, tenant data, customer data, or personal information.</p>
      <p class="fb-privacy" id={modalDescId}>We only send the message you type, the category, the page, and the time. We never ask for your name, email, tenant, or credentials.</p>
      <p class="fb-error" data-fb-error hidden></p>
      <div class="fb-actions">
        <button class="fb-cancel" type="button" data-fb-cancel>Cancel</button>
        <button class="fb-submit" type="button" data-fb-submit disabled>Send feedback</button>
      </div>
    </div>
  </div>
</article>

<script>
  import '../scripts/stars-client';
  import '../scripts/detail-client';
  import '../scripts/detail-rail';
  import '../scripts/feedback-client';
</script>

<style>
  .lens-detail {
    max-width: 1160px;
  }

  /* ---------- Mode B strip ---------- */
  .ld-strip {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--sp-3);
    flex-wrap: wrap;
    padding: var(--sp-3) var(--sp-4);
    margin-bottom: var(--sp-4);
    background: var(--surface-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow-1);
  }
  .ld-strip[hidden],
  .ld-dashboard[hidden],
  .ld-marketing[hidden] {
    display: none !important;
  }
  .ld-strip-id { display: inline-flex; align-items: center; gap: var(--sp-3); min-width: 0; }
  .ld-strip-name { font-size: 1rem; font-weight: 650; color: var(--ink); }
  .ld-strip-right { display: inline-flex; align-items: center; gap: var(--sp-3); }
  .ld-about-tab {
    display: inline-flex; align-items: center; padding: 6px 14px;
    font-size: 0.85rem; font-weight: 600; color: var(--accent);
    background: var(--accent-subtle); border: 1px solid var(--accent);
    border-radius: 999px; cursor: pointer;
    transition: background-color var(--dur) var(--ease), color var(--dur) var(--ease);
  }
  .ld-about-tab:hover { background: var(--accent); color: #fff; }

  /* ---------- Mode B empty state ---------- */
  .ld-dashboard { margin-bottom: var(--sp-5); }
  .ld-empty {
    display: flex; flex-direction: column; align-items: center; text-align: center;
    gap: var(--sp-3); min-height: 460px; justify-content: center;
    padding: var(--sp-6) var(--sp-5);
    background: var(--surface-card);
    border: 1px solid var(--border); border-radius: var(--radius);
    box-shadow: var(--shadow-1);
  }
  .ld-empty-icon {
    display: inline-flex; align-items: center; justify-content: center;
    width: 76px; height: 76px; border-radius: 999px;
    color: var(--tile-c);
    background: color-mix(in srgb, var(--tile-c) 14%, var(--surface-card));
    border: 1px solid color-mix(in srgb, var(--tile-c) 30%, transparent);
    margin-bottom: var(--sp-2);
  }
  .ld-empty-title { font-size: 1.4rem; font-weight: 650; margin: 0; color: var(--ink); }
  .ld-empty-text { font-size: 1rem; line-height: 1.55; color: var(--muted); margin: 0; max-width: 52ch; }
  .ld-empty-actions { display: inline-flex; align-items: center; gap: var(--sp-4); flex-wrap: wrap; justify-content: center; margin-top: var(--sp-3); }
  .ld-empty-art {
    display: inline-flex; align-items: center; justify-content: center;
    width: 132px; height: 132px; border-radius: 999px;
    color: var(--tile-c);
    background: color-mix(in srgb, var(--tile-c) 12%, var(--surface-card));
    border: 1px solid color-mix(in srgb, var(--tile-c) 22%, transparent);
    margin-bottom: var(--sp-3);
  }
  .ld-empty-link {
    display: inline-flex; align-items: center; gap: 6px; margin-top: var(--sp-3);
    font-size: 0.92rem; font-weight: 600; color: var(--accent); text-decoration: none; cursor: pointer;
  }
  .ld-empty-link:hover { text-decoration: underline; }

  /* ---------- Hero ---------- */
  .ld-header-row {
    display: flex; align-items: flex-start; gap: var(--sp-5);
    margin-bottom: var(--sp-4);
  }
  .ld-header-text { flex: 1 1 auto; min-width: 0; }
  .ld-hero {
    position: relative; flex: 0 0 auto; width: 300px; aspect-ratio: 1672 / 941;
    border-radius: var(--radius);
    overflow: hidden; background: #f9f2e9;
  }
  html[data-theme='dark'] .ld-hero { background: #0d111a; }
  .ld-hero-img { display: block; width: 100%; height: 100%; object-fit: contain; }
  .ld-hero-img.ld-hero-dark { display: none; }
  html[data-theme='dark'] .ld-hero-img.ld-hero-light { display: none; }
  html[data-theme='dark'] .ld-hero-img.ld-hero-dark { display: block; }

  /* ---------- Two-column layout ---------- */
  .ld-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 296px;
    gap: var(--sp-6);
    align-items: start;
  }
  .ld-main { max-width: 760px; min-width: 0; }

  .ld-title-top { display: flex; align-items: center; gap: var(--sp-3); flex-wrap: wrap; }
  .ld-title-top h1 { font-size: 2rem; font-weight: 650; letter-spacing: -0.01em; margin: 0; }
  .ld-cat {
    margin: var(--sp-2) 0 0; font-size: 0.8rem; font-weight: 600;
    letter-spacing: 0.06em; text-transform: uppercase; color: var(--subtle);
  }
  .ld-intro { font-size: 1.15rem; line-height: 1.55; color: var(--ink); margin: 0 0 var(--sp-5); padding: 0 calc(var(--sp-5) + 1px); }

  /* ---------- Section cards ---------- */
  .ld-card {
    padding: var(--sp-5);
    margin: 0 0 var(--sp-4);
    background: var(--surface-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow-1);
    scroll-margin-top: 84px;
  }
  .ld-card-head { display: flex; align-items: center; gap: var(--sp-3); margin-bottom: var(--sp-3); }
  .ld-card-icon {
    display: inline-flex; align-items: center; justify-content: center;
    width: 34px; height: 34px; flex: 0 0 auto; border-radius: 9px;
    color: var(--accent); background: var(--accent-subtle);
  }
  .ld-card-head h2 { font-size: 1.2rem; font-weight: 650; margin: 0; color: var(--ink); }
  .ld-prose { font-size: 1rem; line-height: 1.6; color: var(--muted); margin: 0; }
  .ld-get-intro { margin-bottom: var(--sp-4); }
  #ld-top { scroll-margin-top: 20px; }

  /* ---------- What's inside grid ---------- */
  .ld-wi { display: flex; flex-direction: column; gap: var(--sp-4); }
  .ld-wi-label { font-size: 0.9rem; font-weight: 600; color: var(--ink); margin: 0 0 var(--sp-2); }
  .ld-wi-grid {
    list-style: none; margin: 0; padding: 0;
    display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: var(--sp-2);
  }
  .ld-wi-item {
    font-size: 0.9rem; color: var(--muted);
    padding: 8px 12px; border: 1px solid var(--border); border-radius: 8px;
    background: var(--surface-page);
  }

  /* ---------- Get this lens tiers ---------- */
  .ld-tiers { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(210px, 1fr)); gap: var(--sp-4); }
  .ld-tier {
    display: flex; flex-direction: column; gap: var(--sp-3);
    padding: var(--sp-4); background: var(--surface-page);
    border: 1px solid var(--border); border-top: 3px solid var(--accent);
    border-radius: var(--radius);
  }
  .ld-tier--ta { border-top-color: var(--lens-green); }
  .ld-tier--tb { border-top-color: var(--accent); }
  .ld-tier--tc { border-top-color: var(--lens-purple); }
  .ld-tier-head { display: flex; align-items: center; gap: var(--sp-2); flex-wrap: wrap; }
  .ld-tier-icon { display: inline-flex; align-items: center; justify-content: center; width: 34px; height: 34px; flex: 0 0 auto; border-radius: 8px; color: var(--accent); background: var(--accent-subtle); }
  .ld-tier--ta .ld-tier-icon { color: var(--lens-green); background: color-mix(in srgb, var(--lens-green) 14%, var(--surface-card)); }
  .ld-tier--tc .ld-tier-icon { color: var(--lens-purple); background: color-mix(in srgb, var(--lens-purple) 14%, var(--surface-card)); }
  .ld-tier-name { font-size: 1rem; font-weight: 650; color: var(--ink); margin: 0; }
  .ld-tier-desc { font-size: 0.9rem; line-height: 1.45; color: var(--muted); margin: 0; }
  .ld-tier .ld-checks { flex: 1 1 auto; }
  .ld-tier .ld-dl-btn { margin-top: auto; width: 100%; justify-content: center; }
  .ld-dl-btn {
    display: inline-flex; align-items: center; gap: 6px; flex: 0 0 auto;
    padding: 8px 16px; font-size: 0.88rem; font-weight: 600;
    color: #fff; background: var(--accent); border: 1px solid var(--accent);
    border-radius: 8px; text-decoration: none;
    transition: background-color var(--dur) var(--ease), transform var(--dur-fast) var(--ease);
  }
  .ld-dl-btn:hover { filter: brightness(1.05); }
  .ld-dl-btn:active { transform: translateY(1px); }

  .ld-disclaimer {
    margin: var(--sp-5) 0 0; padding-top: var(--sp-4);
    border-top: 1px solid var(--border);
    font-size: 0.85rem; line-height: 1.5; color: var(--subtle);
  }

  /* ---------- Right rail ---------- */
  .ld-rail { position: sticky; top: 76px; display: flex; flex-direction: column; gap: var(--sp-4); }
  .ld-rail-card {
    padding: var(--sp-4); background: var(--surface-card);
    border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow-1);
  }
  .ld-rail-title {
    font-size: 0.72rem; font-weight: 700; letter-spacing: 0.07em; text-transform: uppercase;
    color: var(--subtle); margin: 0 0 var(--sp-3);
  }
  .ld-rail-nav ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 2px; }
  .ld-rail-nav a {
    display: block; padding: 6px 10px; font-size: 0.9rem; color: var(--muted);
    text-decoration: none; border-left: 2px solid transparent; border-radius: 0 6px 6px 0;
    transition: color var(--dur) var(--ease), background-color var(--dur) var(--ease);
  }
  .ld-rail-nav a:hover { color: var(--ink); background: var(--surface-page); }
  .ld-rail-nav a.is-active { color: var(--accent); border-left-color: var(--accent); background: var(--accent-subtle); font-weight: 600; }

  .ld-status-line { margin: 0 0 var(--sp-3); }
  .ld-meta { margin: 0; display: flex; flex-direction: column; gap: var(--sp-2); }
  .ld-meta div { display: flex; align-items: baseline; justify-content: space-between; gap: var(--sp-3); }
  .ld-meta dt { font-size: 0.85rem; color: var(--subtle); margin: 0; }
  .ld-meta dd { font-size: 0.9rem; font-weight: 600; color: var(--ink); margin: 0; }

  .ld-fb-card .star-row { margin-bottom: var(--sp-3); }
  .ld-fb-star-label { font-size: 0.85rem; color: var(--subtle); }
  .ld-fb-btn {
    display: inline-flex; align-items: center; justify-content: center; width: 100%;
    padding: 8px 14px; font-size: 0.88rem; font-weight: 600;
    color: var(--accent); background: var(--accent-subtle);
    border: 1px solid var(--accent); border-radius: 8px; cursor: pointer;
    transition: background-color var(--dur) var(--ease), color var(--dur) var(--ease);
  }
  .ld-fb-btn:hover { background: var(--accent); color: #fff; }
  .ld-fb-btn--quiet { width: auto; }
  .ld-fb-status { display: block; margin-top: var(--sp-2); font-size: 0.82rem; color: var(--lens-green); }
  .ld-empty-actions .ld-fb-status { margin-top: 0; }

  /* ---------- Star row ---------- */
  .star-row { display: inline-flex; align-items: center; gap: 6px; }
  .star-btn {
    display: inline-flex; align-items: center; justify-content: center;
    width: 26px; height: 26px; border: 1px solid var(--border-strong);
    border-radius: 999px; background: var(--surface-card); color: var(--accent);
    font-size: 15px; line-height: 1; cursor: pointer;
    transition: background-color var(--dur) var(--ease), border-color var(--dur) var(--ease), transform var(--dur-fast) var(--ease);
  }
  .star-btn:hover { background: var(--accent-subtle); border-color: var(--accent); }
  .star-btn:active { transform: translateY(1px); }
  .star-btn.is-starred { background: var(--accent-subtle); border-color: var(--accent); }
  .star-glyph { pointer-events: none; }
  .star-count { min-width: 1ch; font-size: 0.9rem; font-weight: 600; color: var(--ink); }

  /* ---------- Feedback modal ---------- */
  .fb-modal[hidden] { display: none; }
  .fb-modal { position: fixed; inset: 0; z-index: 200; display: flex; align-items: center; justify-content: center; padding: var(--sp-4); }
  .fb-backdrop { position: absolute; inset: 0; background: rgba(8, 12, 20, 0.55); backdrop-filter: blur(2px); }
  .fb-dialog {
    position: relative; width: 100%; max-width: 480px; max-height: 90vh; overflow-y: auto;
    background: var(--surface-card); border: 1px solid var(--border);
    border-radius: var(--radius); box-shadow: var(--shadow-2); padding: var(--sp-5);
  }
  .fb-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: var(--sp-3); }
  .fb-title { font-size: 1.2rem; font-weight: 650; margin: 0; color: var(--ink); }
  .fb-x { background: none; border: none; color: var(--muted); font-size: 1rem; cursor: pointer; padding: 4px; line-height: 1; }
  .fb-x:hover { color: var(--ink); }
  .fb-label { display: block; font-size: 0.82rem; font-weight: 600; color: var(--ink); margin: var(--sp-3) 0 6px; }
  .fb-select, .fb-textarea {
    width: 100%; font: inherit; font-size: 0.95rem; color: var(--ink);
    background: var(--surface-page); border: 1px solid var(--border-strong);
    border-radius: 8px; padding: 8px 10px;
  }
  .fb-textarea { resize: vertical; min-height: 110px; line-height: 1.5; }
  .fb-select:focus, .fb-textarea:focus { outline: 2px solid var(--accent); outline-offset: 1px; border-color: var(--accent); }
  .fb-meta { display: flex; justify-content: flex-end; margin-top: 4px; }
  .fb-counter { font-size: 0.78rem; color: var(--subtle); }
  .fb-counter--bad { color: var(--lens-red, #c23b3b); }
  .fb-safety, .fb-privacy { font-size: 0.8rem; line-height: 1.45; color: var(--subtle); margin: var(--sp-3) 0 0; }
  .fb-error {
    font-size: 0.85rem; color: var(--lens-red, #c23b3b); margin: var(--sp-3) 0 0;
    padding: 8px 10px; background: color-mix(in srgb, var(--lens-red, #c23b3b) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--lens-red, #c23b3b) 30%, transparent); border-radius: 8px;
  }
  .fb-error[hidden] { display: none; }
  .fb-actions { display: flex; align-items: center; justify-content: flex-end; gap: var(--sp-3); margin-top: var(--sp-4); }
  .fb-cancel {
    padding: 8px 16px; font-size: 0.88rem; font-weight: 600; color: var(--muted);
    background: none; border: 1px solid var(--border-strong); border-radius: 8px; cursor: pointer;
  }
  .fb-cancel:hover { color: var(--ink); border-color: var(--ink); }
  .fb-submit {
    padding: 8px 18px; font-size: 0.88rem; font-weight: 600; color: #fff;
    background: var(--accent); border: 1px solid var(--accent); border-radius: 8px; cursor: pointer;
  }
  .fb-submit:disabled { opacity: 0.5; cursor: not-allowed; }

  /* ---------- Section icon tints (fix6) ---------- */
  .ld-icon-shows { color: var(--lens-purple); background: color-mix(in srgb, var(--lens-purple) 14%, var(--surface-card)); }
  .ld-icon-inside { color: var(--accent); background: var(--accent-subtle); }
  .ld-icon-data { color: var(--lens-green); background: color-mix(in srgb, var(--lens-green) 15%, var(--surface-card)); }
  .ld-icon-get { color: var(--lens-orange); background: color-mix(in srgb, var(--lens-orange) 15%, var(--surface-card)); }

  /* ---------- Stat strip ---------- */
  .ld-stats { display: flex; align-items: stretch; margin: 0 0 var(--sp-5); padding: var(--sp-4) var(--sp-5); background: var(--surface-card); border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow-1); }
  .ld-stat { flex: 1 1 0; display: flex; flex-direction: column; gap: 3px; padding: 0 var(--sp-4); border-left: 1px solid var(--border); min-width: 0; }
  .ld-stat:first-child { border-left: none; padding-left: 0; }
  .ld-stat:last-child { padding-right: 0; }
  .ld-stat-value { font-size: 1.6rem; font-weight: 700; letter-spacing: -0.01em; color: var(--ink); line-height: 1.1; }
  .ld-stat-label { font-size: 0.82rem; line-height: 1.35; color: var(--muted); }

  /* ---------- Section lead + note ---------- */
  .ld-lead { font-size: 1rem; line-height: 1.55; color: var(--muted); margin: 0 0 var(--sp-4); }
  .ld-note { display: flex; align-items: flex-start; gap: 6px; font-size: 0.85rem; line-height: 1.45; color: var(--subtle); margin: var(--sp-3) 0 0; }
  .ld-note :global(svg) { flex: 0 0 auto; margin-top: 1px; }

  /* ---------- Feature grid ---------- */
  .ld-feature-grid { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: repeat(auto-fill, minmax(210px, 1fr)); gap: var(--sp-3); }
  .ld-feature { display: flex; gap: var(--sp-3); padding: var(--sp-3) var(--sp-4); border: 1px solid var(--border); border-radius: 10px; background: var(--surface-page); }
  .ld-feature-icon { display: inline-flex; align-items: center; justify-content: center; width: 32px; height: 32px; flex: 0 0 auto; border-radius: 8px; color: var(--accent); background: var(--accent-subtle); }
  .ld-feature-body { min-width: 0; }
  .ld-feature-title { font-size: 0.95rem; font-weight: 650; color: var(--ink); margin: 0 0 2px; }
  .ld-feature-desc { font-size: 0.85rem; line-height: 1.45; color: var(--muted); margin: 0; }

  /* ---------- Value flow ---------- */
  .ld-flow-wrap { margin-top: var(--sp-4); padding-top: var(--sp-4); border-top: 1px solid var(--border); }
  .ld-flow-title { font-size: 0.9rem; font-weight: 650; color: var(--ink); margin: 0 0 var(--sp-3); }
  .ld-flow { list-style: none; margin: 0; padding: 0; display: flex; flex-wrap: wrap; align-items: center; gap: var(--sp-2); }
  .ld-flow-step { display: inline-flex; align-items: center; gap: var(--sp-2); }
  .ld-flow-node { display: inline-flex; align-items: center; padding: 6px 12px; font-size: 0.82rem; font-weight: 600; color: var(--ink); background: var(--surface-page); border: 1px solid var(--border-strong); border-radius: 999px; white-space: nowrap; }
  .ld-flow-arrow { display: inline-flex; color: var(--subtle); }

  /* ---------- What's inside page grid ---------- */
  .ld-pages { display: flex; flex-direction: column; gap: var(--sp-4); }
  .ld-group-label { font-size: 0.8rem; font-weight: 700; letter-spacing: 0.05em; text-transform: uppercase; color: var(--subtle); margin: 0 0 var(--sp-2); }
  .ld-page-grid { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: var(--sp-2); }
  .ld-page-item { display: flex; align-items: center; gap: var(--sp-2); padding: 10px 12px; border: 1px solid var(--border); border-radius: 8px; background: var(--surface-page); }
  .ld-page-icon { display: inline-flex; align-items: center; justify-content: center; flex: 0 0 auto; color: var(--accent); }
  .ld-page-name { font-size: 0.88rem; color: var(--ink); min-width: 0; }

  /* ---------- Build cards ---------- */
  .ld-build-cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: var(--sp-4); }
  .ld-build-card { padding: var(--sp-4); border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface-page); }
  .ld-build-head { display: flex; align-items: center; gap: var(--sp-2); margin-bottom: var(--sp-3); flex-wrap: wrap; }
  .ld-build-icon { display: inline-flex; align-items: center; justify-content: center; width: 30px; height: 30px; flex: 0 0 auto; border-radius: 8px; color: var(--accent); background: var(--accent-subtle); }
  .ld-build-name { font-size: 1rem; font-weight: 650; color: var(--ink); margin: 0; }

  /* ---------- Badge ---------- */
  .ld-badge { display: inline-flex; align-items: center; padding: 2px 9px; font-size: 0.7rem; font-weight: 700; letter-spacing: 0.02em; text-transform: uppercase; color: var(--accent); background: var(--accent-subtle); border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent); border-radius: 999px; }

  /* ---------- Checkmark lists ---------- */
  .ld-checks { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: var(--sp-2); }
  .ld-check { display: flex; align-items: flex-start; gap: var(--sp-2); font-size: 0.88rem; line-height: 1.4; color: var(--muted); }
  .ld-check-icon { display: inline-flex; flex: 0 0 auto; color: var(--lens-green); margin-top: 1px; }

  /* ---------- Data requirements table ---------- */
  .ld-table { border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; }
  .ld-tr { display: grid; grid-template-columns: minmax(140px, 1.2fr) minmax(0, 2fr) auto; align-items: center; gap: var(--sp-3); padding: var(--sp-3) var(--sp-4); border-top: 1px solid var(--border); }
  .ld-tr:first-child { border-top: none; }
  .ld-tr-head { background: var(--surface-page); }
  .ld-th { font-size: 0.75rem; font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase; color: var(--subtle); }
  .ld-th-req, .ld-td-req { text-align: right; justify-self: end; }
  .ld-td { font-size: 0.88rem; color: var(--muted); line-height: 1.4; min-width: 0; }
  .ld-td-source { display: flex; align-items: center; gap: var(--sp-2); }
  .ld-src-icon { display: inline-flex; flex: 0 0 auto; color: var(--accent); }
  .ld-src-name { font-weight: 600; color: var(--ink); }
  .ld-req-pill { display: inline-flex; align-items: center; padding: 3px 10px; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.02em; border-radius: 999px; white-space: nowrap; }
  .ld-req-required { color: var(--lens-green); background: color-mix(in srgb, var(--lens-green) 14%, var(--surface-card)); border: 1px solid color-mix(in srgb, var(--lens-green) 35%, transparent); }
  .ld-req-optional { color: var(--subtle); background: var(--surface-page); border: 1px solid var(--border-strong); }
  .ld-tr-note { background: var(--surface-page); }
  .ld-td-note { grid-column: 1 / -1; display: flex; align-items: flex-start; gap: 6px; font-size: 0.82rem; line-height: 1.4; color: var(--subtle); }
  .ld-td-note :global(svg) { flex: 0 0 auto; margin-top: 1px; }

  /* ---------- fix6 responsive ---------- */
  @media (max-width: 560px) {
    .ld-stats { flex-direction: column; }
    .ld-stat { border-left: none; padding: var(--sp-3) 0 0; border-top: 1px solid var(--border); }
    .ld-stat:first-child { border-top: none; padding-top: 0; }
    .ld-tr { grid-template-columns: 1fr; gap: 6px; }
    .ld-tr-head { display: none; }
    .ld-th-req, .ld-td-req { justify-self: start; text-align: left; }
  }

  /* ---------- Responsive: rail reflows below content ---------- */
  @media (max-width: 900px) {
    .ld-layout { grid-template-columns: minmax(0, 1fr); }
    .ld-rail { position: static; flex-direction: column; }
    .ld-main { max-width: none; }
  }
  @media (max-width: 640px) {
    .ld-strip { align-items: flex-start; }
    .ld-header-row { flex-direction: column; }
    .ld-hero { width: 100%; max-width: 360px; }
  }
</style>
