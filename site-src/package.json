{
  "name": "site-src",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build:astro": "astro build",
    "build:screenshot-manifest": "node scripts/build-screenshot-manifest.mjs",
    "build:search": "pagefind --site dist",
    "build": "npm run build:astro && npm run build:screenshot-manifest && npm run build:search",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.2",
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "parse5": "^8.0.1",
    "playwright": "^1.61.1"
  }
}
